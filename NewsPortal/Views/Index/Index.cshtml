
@{
    ViewBag.Title = "Index";
    Layout = "~/Views/_LayoutPage1.cshtml";
}

<!-- Features Start -->
<div class="container-fluid features mb-5">
    <div class="container py-5">
        <div class="row g-4">
            @foreach (var item in ViewBag.rightSideNews)
            {
                <div class="col-md-6 col-lg-6 col-xl-3">
                    <div class="row g-4 align-items-center features-item">
                        <div class="col-4">
                            <div class="position-relative">
                                <div class="overflow-hidden">
                                    @if (@item.image != null)
                                    {
                                        var base64 = Convert.ToBase64String(@item.image);
                                        var imgsrc = string.Format("data:image/jpg;base64,{0}", base64);

                                        <img alt="@item.image" src='@imgsrc' class="img-zoomin img-fluid rounded w-100" />
                                    }
                                </div>
                                <!--<span class="border border-2 border-white bg-primary btn-sm-square text-white position-absolute rounded" style="top: 10%; right: -10px; font-size: 9pt">@item.views</span>-->
                            </div>
                        </div>
                        <div class="col-8">
                            <div class="features-content d-flex flex-column">
                                <p class="text-uppercase mb-0" style="font-size: 16pt">@item.Category.name</p>
                                @Html.ActionLink((string)@item.title, "Details", "Posts", new { id = @item.id }, new { @class = "h5 mb-0" })
                                <small class="text-body d-block" style="font-size: 9pt"><i class="fas fa-calendar-alt me-1"></i>@item.RecordDate.ToString("dd-MM-yyyy")</small>
                            </div>
                        </div>
                    </div>
                </div>
            }

        </div>
    </div>
</div>
<!-- Features End -->
<!-- Main Post Section Start -->
<div class="container-fluid" style="padding-bottom: 3rem !important;">
    <div class="container">
        <div class="row g-4">
            <div class="col-lg-7 col-xl-8 mt-0">
                <div class="position-relative overflow-hidden rounded">

                    @if (ViewBag.Image != null)
                    {
                        <hr />
                        <img alt="" src="@ViewBag.Image" class="img-fluid rounded img-zoomin w-100" alt="" />
                    }


                    <div class="d-flex justify-content-center px-4 position-absolute flex-wrap" style="bottom: 10px; left: 0;">
                        <!--<a href="#" class="text-white me-3 link-hover"><i class="fa fa-clock"></i> 06 minute read</a>
        <a href="#" class="text-white me-3 link-hover"><i class="fa fa-eye"></i> 3.5k Views</a>
        <a href="#" class="text-white me-3 link-hover"><i class="fa fa-comment-dots"></i> 05 Comment</a>
        <a href="#" class="text-white link-hover"><i class="fa fa-arrow-up"></i> 1.5k Share</a>-->
                    </div>
                </div>
                <div class="border-bottom py-3">
                    @Html.ActionLink((string)ViewBag.MainNews.title, "Details", "Posts", new { id = ViewBag.MainNews.id }, new { @class = "display-6 text-dark mb-0 link-hover" })
                </div>
                <p class="mt-3 mb-4">
                    @Html.ActionLink((string)ViewBag.Slug, "Details", "Posts", new { id = ViewBag.MainNews.id }, new { @class = "text-dark mb-0 link-hover" })
                </p>
                <div class="bg-light p-4 rounded">
                    <div class="news-2">
                        <h3 class="mb-4">এক্সক্লুসিভ</h3>
                    </div>
                    <div class="row g-4 align-items-center">
                        <div class="col-md-6">
                            <div class="rounded overflow-hidden">
                                <img src="@ViewBag.ExclusiveNewsImage" class="img-fluid rounded img-zoomin w-100" alt="">
                            </div>
                        </div>
                        <div class="col-md-6">
                            <div class="d-flex flex-column">
                                @Html.ActionLink((string)@ViewBag.ExclusiveNews.title, "Details", "Posts", new { id = ViewBag.ExclusiveNews.id }, new { @class = "h5" })
                                <p class="h7">
                                    @Html.ActionLink((string)@ViewBag.SlugExclusiveNews, "Details", "Posts", new { id = ViewBag.ExclusiveNews.id }, new { @class = "text-dark mb-0 link-hover" })
                                </p>
                                <p class="h4"></p>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
            <div class="col-lg-5 col-xl-4">
                <div class="bg-light rounded p-4 pt-0">
                    <div class="row g-4">
                        <div class="col-12">
                            <div class="rounded overflow-hidden">
                                <img src="@ViewBag.SubImage" class="img-fluid rounded img-zoomin w-100" alt="">
                            </div>
                        </div>
                        <div class="col-12">
                            <div class="d-flex flex-column">
                                @Html.ActionLink((string)ViewBag.SubNews.title, "Details", "Posts", new { id = ViewBag.SubNews.id }, new { @class = "h4 mb-2" })
                                <p class="fs-5 mb-0"></p>
                                <p class="fs-5 mb-0"></p>
                            </div>
                        </div>

                        @foreach (var item in ViewBag.rightSideNews)
                        {
                            <div class="col-12">
                                <div class="row g-4 align-items-center">
                                    <div class="col-5">
                                        <div class="overflow-hidden rounded">
                                            @if (@item.image != null)
                                            {
                                                var base64 = Convert.ToBase64String(@item.image);
                                                var imgsrc = string.Format("data:image/jpg;base64,{0}", base64);

                                                <img alt="@item.image" src='@imgsrc' class="img-zoomin img-fluid rounded w-100" />
                                            }
                                        </div>
                                    </div>
                                    <div class="col-7">
                                        <div class="features-content d-flex flex-column">
                                                @Html.ActionLink((string)@item.title, "Details", "Posts", new { id = @item.id }, new { @class = "h5 mb-0" })
                                            <small>
                                                <i class="fa fa-eye" style="font-size: 10pt; font-weight: 400">
                                                    <span style="font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif; font-size: 10pt; font-weight: 400">@item.views views</span>
                                                </i>
                                            </small>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        }
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
<!-- Main Post Section End -->
<!-- Banner Start -->
<!--<div class="container-fluid py-5 my-5" style="background: linear-gradient(rgba(202, 203, 185, 1), rgba(202, 203, 185, 1));">
    <div class="container">
        <div class="row g-4 align-items-center">
            <div class="col-lg-7">
                <h1 class="mb-4 text-primary">Newsers</h1>
                <h1 class="mb-4">Get Every Weekly Updates</h1>
                <p class="text-dark mb-4 pb-2">
                    Lorem Ipsum is simply dummy text of the printing and typesetting industry. Lorem Ipsum has been the industry's standard dummy text ever since the 1500s, when an unknown printer took a galley
                </p>
                <div class="position-relative mx-auto">
                    <input class="form-control w-100 py-3 rounded-pill" type="email" placeholder="Your Busines Email">
                    <button type="submit" class="btn btn-primary py-3 px-5 position-absolute rounded-pill text-white h-100" style="top: 0; right: 0;">Subscribe Now</button>
                </div>
            </div>
            <div class="col-lg-5">
                <div class="rounded">
                    <img src="~/assest/img/banner-img.jpg" class="img-fluid rounded w-100 rounded" alt="">
                </div>
            </div>
        </div>
    </div>
</div>-->
<!-- Banner End -->
<!-- Latest News Start -->
<div class="container-fluid latest-news py-5">
    <div class="container py-5">
        <h2 class="mb-4">সর্বশেষ সংবাদ</h2>
        <div class="latest-news-carousel owl-carousel">
            @foreach (var item in ViewBag.rightSideNews)
            {
                <div class="latest-news-item">
                    <div class="bg-light rounded">
                        <div class="rounded-top overflow-hidden">
                            @if (@item.image != null)
                            {
                                var base64 = Convert.ToBase64String(@item.image);
                                var imgsrc = string.Format("data:image/jpg;base64,{0}", base64);

                                <img alt="@item.image" src='@imgsrc' class="img-zoomin img-fluid rounded-top w-100" alt="">
                            }
                        </div>
                        <div class="d-flex flex-column p-4">
                            <a href="#" class="h4">@item.Category.name</a>
                            <div class="d-flex justify-content-between">
                                @Html.ActionLink((string)@item.title, "Details", "Posts", new { id = @item.id }, new { @class = "h5 mb-0" })  
                            </div>
                            <div class="col-8">
                                <div class="features-content d-flex flex-column">                             
                                    <small class="text-body d-block" style="font-size: 9pt"><i class="fas fa-calendar-alt me-1"></i>@item.RecordDate.ToString("dd-MM-yyyy")</small>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            }

        </div>
    </div>
</div>
<!-- Latest News End -->
<!-- Most Populer News Start -->
<div class="container-fluid populer-news py-5">
    <div class="container py-5">
        <div class="tab-class mb-4">
            <div class="row g-4">
                <div class="col-lg-8 col-xl-9">
                    <div class="d-flex flex-column flex-md-row justify-content-md-between border-bottom mb-4">
                        <h1 class="mb-4">সাম্প্রতিক খবর</h1>
                        <ul class="nav nav-pills d-inline-flex text-center">
                            <li class="nav-item mb-3">
                                <a class="d-flex py-2 bg-light rounded-pill active me-2" data-bs-toggle="pill" href="#tab-1">
                                    <span class="text-dark" style="width: 100px;">খেলাধুলা</span>
                                </a>
                            </li>
                            <li class="nav-item mb-3">
                                <a class="d-flex py-2 bg-light rounded-pill me-2" data-bs-toggle="pill" href="#tab-2">
                                    <span class="text-dark" style="width: 100px;">ম্যাগাজিন</span>
                                </a>
                            </li>
                            <li class="nav-item mb-3">
                                <a class="d-flex py-2 bg-light rounded-pill me-2" data-bs-toggle="pill" href="#tab-3">
                                    <span class="text-dark" style="width: 100px;">রাজনীতি</span>
                                </a>
                            </li>
                            <li class="nav-item mb-3">
                                <a class="d-flex py-2 bg-light rounded-pill me-2" data-bs-toggle="pill" href="#tab-4">
                                    <span class="text-dark" style="width: 100px;">প্রযুক্তি</span>
                                </a>
                            </li>
                            <li class="nav-item mb-3">
                                <a class="d-flex py-2 bg-light rounded-pill me-2" data-bs-toggle="pill" href="#tab-5">
                                    <span class="text-dark" style="width: 100px;">ফ্যাশন</span>
                                </a>
                            </li>
                        </ul>
                    </div>
                    <div class="tab-content mb-4">
                        <div id="tab-1" class="tab-pane fade show p-0 active">
                            <div class="row g-4">
                                @foreach(var item in ViewBag.sportsimg)
                                    {
                                        <div class="col-lg-8">
                                            <div class="position-relative rounded overflow-hidden">
                                                @if (@item.image != null)
                                                {
                                                    var base64 = Convert.ToBase64String(@item.image);
                                                    var imgsrc = string.Format("data:image/jpg;base64,{0}", base64);

                                                    <img alt="@item.image" src='@imgsrc' class="img-zoomin img-fluid rounded w-100" alt="">
                                                }
                                                
                                                <div class="position-absolute text-white px-4 py-2 bg-primary rounded" style="top: 20px; right: 20px;">
                                                    খেলাধুলা
                                                </div>
                                            </div>
                                            <div class="my-4">
                                                @Html.ActionLink((string)@item.title, "Details", "Posts", new { id = @item.id }, new { @class = "h5 mb-0" })
                                            </div>
                                            <!--<div class="d-flex justify-content-between">
                                            <a href="#" class="text-dark link-hover me-3"><i class="fa fa-clock"></i> 06 minute read</a>
                                            <a href="#" class="text-dark link-hover me-3"><i class="fa fa-eye"></i> 3.5k Views</a>
                                            <a href="#" class="text-dark link-hover me-3"><i class="fa fa-comment-dots"></i> 05 Comment</a>
                                            <a href="#" class="text-dark link-hover"><i class="fa fa-arrow-up"></i> 1.5k Share</a>
                                            </div>-->
                                            <p class="my-4">
                                                @Html.ActionLink((string)@item.slug, "Details", "Posts", new { id = @item.id }, new { @class = "text-dark mb-0 link-hover" })
                                            </p>
                                        </div>
                                    }
                                
                                <div class="col-lg-4">
                                    <div class="row g-4">
                                        <div class="col-12">
                                            @foreach (var item in ViewBag.sports)
                                            {
                                                <div class="row g-4 align-items-center">
                                                    <div class="col-5">
                                                        @if (@item.image != null)
                                                        {
                                                            var base64 = Convert.ToBase64String(@item.image);
                                                            var imgsrc = string.Format("data:image/jpg;base64,{0}", base64);

                                                            <img alt="@item.image" src='@imgsrc' class="img-zoomin img-fluid rounded w-100" alt="">
                                                        }
                                                        
                                                    </div>
                                                    <div class="col-7">
                                                        <div class="features-content d-flex flex-column">
                                                            <p class="text-uppercase mb-2">@item.Category.name</p>
                                                            @Html.ActionLink((string)@item.title, "Details", "Posts", new { id = @item.id }, new { @class = "h5 mb-0" })
                                                            <small class="text-body d-block"><i class="fas fa-calendar-alt me-1"></i> @item.RecordDate.ToString("dd-MM-yyyy")</small>
                                                        </div>
                                                    </div>
                                                </div>
                                            }
                                            </div>
                                       
                                    </div>
                                </div>
                            </div>
                        </div>
                        <div id="tab-2" class="tab-pane fade show p-0">
                            <div class="row g-4">
                                @foreach(var item in ViewBag.entertaintmentimg)
                                    {
                                        <div class="col-lg-8">
                                            <div class="position-relative rounded overflow-hidden">
                                                @if (@item.image != null)
                                                {
                                                    var base64 = Convert.ToBase64String(@item.image);
                                                    var imgsrc = string.Format("data:image/jpg;base64,{0}", base64);

                                                    <img alt="@item.image" src='@imgsrc' class="img-zoomin img-fluid rounded w-100" alt="">
                                                }
                                                
                                                <div class="position-absolute text-white px-4 py-2 bg-primary rounded" style="top: 20px; right: 20px;">
                                                    ম্যাগাজিন
                                                </div>
                                            </div>
                                            <div class="my-3">
                                                @Html.ActionLink((string)@item.title, "Details", "post", new {id = item.id}, new {@class ="h5 mb-0"})
                                            </div>
                                            <p class="mt-4">
                                                @Html.ActionLink((string)@item.slug, "Details", "Posts", new { id = @item.id }, new { @class = "text-dark mb-0 link-hover" })
                                            </p>
                                            <div class="d-flex justify-content-between">
                                                <!--<a href="#" class="text-dark link-hover me-3"><i class="fa fa-clock"></i> 06 minute read</a>
                                                <a href="#" class="text-dark link-hover me-3"><i class="fa fa-eye"></i> 3.5k Views</a>
                                                <a href="#" class="text-dark link-hover me-3"><i class="fa fa-comment-dots"></i> 05 Comment</a>
                                                <a href="#" class="text-dark link-hover"><i class="fa fa-arrow-up"></i> 1.5k Share</a>-->
                                            </div>
                                        </div>
                                    }
                                
                                <div class="col-lg-4">
                                    <div class="row g-4">
                                        @foreach (var item in ViewBag.entertaintment)
                                        {
                                        <div class="col-12">
                                            <div class="row g-4 align-items-center">
                                                <div class="col-5">
                                                    <div class="overflow-hidden rounded">
                                                        @if (@item.image != null)
                                                        {
                                                            var base64 = Convert.ToBase64String(@item.image);
                                                            var imgsrc = string.Format("data:image/jpg;base64,{0}", base64);

                                                            <img alt="@item.image" src='@imgsrc' class="img-zoomin img-fluid rounded w-100" alt="">
                                                        }
                                                        
                                                    </div>
                                                </div>
                                                <div class="col-7">
                                                    <div class="features-content d-flex flex-column">
                                                        <p class="text-uppercase mb-2">@item.Category.name</p>
                                                        @Html.ActionLink((string)@item.title, "Details", "Posts", new { id = @item.id }, new { @class = "h5 mb-0" })
                                                    <small class="text-body d-block"><i class="fas fa-calendar-alt me-1"></i>@item.RecordDate.ToString("dd-MM-yyyy")    </small>
                                                    </div>
                                                </div>
                                            </div>
                                        </div>
                                        }
                                       
                                    </div>
                                </div>
                            </div>
                        </div>
                        <div id="tab-3" class="tab-pane fade show p-0">
                            <div class="row g-4">
                                @foreach(var item in ViewBag.politicsimg)
                                    {
                                        <div class="col-lg-8">
                                            <div class="position-relative rounded overflow-hidden">
                                                @if (item.image != null)
                                                {
                                                    var base64 = Convert.ToBase64String(item.image);
                                                    var imgsrc = string.Format("data:image/jpg;base64,{0}", base64);
                                                    <img alt="@item.image" src='@imgsrc' class="img-zoomin img-fluid rounded w-100" alt="">
                                                }

                                                
                                                <div class="position-absolute text-white px-4 py-2 bg-primary rounded" style="top: 20px; right: 20px;">
                                                    রাজনীতি
                                                </div>
                                            </div>
                                            <div class="my-3">
                                                @Html.ActionLink((string)@item.title, "Details", "Posts", new { id = @item.id }, new { @class = "h5 mb-0" })
                                            </div>
                                            <p class="mt-4">
                                                @Html.ActionLink((string)@item.slug, "Details", "Posts", new { id = @item.id }, new { @class = "text-dark mb-0 link-hover" })
                                            </p>
                                            <div class="d-flex justify-content-between">
                                                <!--<a href="#" class="text-dark link-hover me-3"><i class="fa fa-clock"></i> 06 minute read</a>
                                                <a href="#" class="text-dark link-hover me-3"><i class="fa fa-eye"></i> 3.5k Views</a>
                                                <a href="#" class="text-dark link-hover me-3"><i class="fa fa-comment-dots"></i> 05 Comment</a>
                                                <a href="#" class="text-dark link-hover"><i class="fa fa-arrow-up"></i> 1.5k Share</a>-->
                                            </div>
                                        </div>
                                    }
                                
                                <div class="col-lg-4">
                                    <div class="row g-4">
                                        @foreach (var item in ViewBag.politics)
                                        {
                                            <div class="col-12">
                                                <div class="row g-4 align-items-center">
                                                    <div class="col-5">
                                                        <div class="overflow-hidden rounded">
                                                            @if (@item.image != null)
                                                            {
                                                                var base64 = Convert.ToBase64String(@item.image);
                                                                var imgsrc = string.Format("data:image/jpg;base64,{0}", base64);

                                                                <img alt="@item.image" src='@imgsrc' class="img-zoomin img-fluid rounded w-100" alt="">
                                                            }
                                                           
                                                        </div>
                                                    </div>
                                                    <div class="col-7">
                                                        <div class="features-content d-flex flex-column">
                                                            <p class="text-uppercase mb-2">@item.Category.name</p>
                                                            @Html.ActionLink((string)@item.title, "Details", "Posts", new { id = @item.id }, new { @class = "h5 mb-0" })
                                                            <small class="text-body d-block"><i class="fas fa-calendar-alt me-1"></i>@item.RecordDate.ToString("dd-MM-yyyy")</small>
                                                        </div>
                                                    </div>
                                                </div>
                                            </div>
                                        }

                                    </div>
                                </div>
                            </div>
                        </div>
                        <div id="tab-4" class="tab-pane fade show p-0">
                            <div class="row g-4">
                                @foreach(var item in ViewBag.technologyimg)
                                    {
                                        <div class="col-lg-8">
                                            <div class="position-relative rounded overflow-hidden">
                                                @if (item.image != null)
                                                {
                                                    var base64 = Convert.ToBase64String(item.image);
                                                    var imgsrc = string.Format("data:image/jpg;base64,{0}", base64);
                                                    <img alt="@item.image" src='@imgsrc' class="img-zoomin img-fluid rounded w-100" alt="">
                                                }
                                               
                                                <div class="position-absolute text-white px-4 py-2 bg-primary rounded" style="top: 20px; right: 20px;">
                                                    প্রযুক্তি
                                                </div>
                                            </div>
                                            <div class="my-3">
                                                @Html.ActionLink((string)@item.title, "Details", "Posts", new { id = @item.id }, new { @class = "h5 mb-0" })
                                            </div>
                                            <p class="mt-4">
                                                @Html.ActionLink((string)@item.slug, "Details", "Posts", new { id = @item.id }, new { @class = "text-dark mb-0 link-hover" })
                                            </p>
                                            <div class="d-flex justify-content-between">
                                                <!--<a href="#" class="text-dark link-hover me-3"><i class="fa fa-clock"></i> 06 minute read</a>
                                                <a href="#" class="text-dark link-hover me-3"><i class="fa fa-eye"></i> 3.5k Views</a>
                                                <a href="#" class="text-dark link-hover me-3"><i class="fa fa-comment-dots"></i> 05 Comment</a>
                                                <a href="#" class="text-dark link-hover"><i class="fa fa-arrow-up"></i> 1.5k Share</a>-->
                                            </div>
                                        </div>
                                    }
                                
                                <div class="col-lg-4">
                                    <div class="row g-4">
                                        @foreach (var item in ViewBag.technology)
                                            {
                                                <div class="col-12">
                                                    <div class="row g-4 align-items-center">
                                                        <div class="col-5">
                                                            <div class="overflow-hidden rounded">
                                                                @if (@item.image != null)
                                                                {
                                                                    var base64 = Convert.ToBase64String(@item.image);
                                                                    var imgsrc = string.Format("data:image/jpg;base64,{0}", base64);

                                                                    <img alt="@item.image" src='@imgsrc' class="img-zoomin img-fluid rounded w-100" alt="">
                                                                }
                                                            </div>
                                                        </div>
                                                        <div class="col-7">
                                                            <div class="features-content d-flex flex-column">
                                                                <p class="text-uppercase mb-2">@item.Category.name</p>
                                                                @Html.ActionLink((string)@item.title, "Details", "Posts", new { id = @item.id }, new { @class = "h5 mb-0" })
                                                                <small class="text-body d-block"><i class="fas fa-calendar-alt me-1"></i>@item.RecordDate.ToString("dd-MM-yyyy")</small>
                                                            </div>
                                                        </div>
                                                    </div>
                                                </div>
                                            }                                    
                                    </div>
                                </div>
                            </div>
                        </div>
                        <div id="tab-5" class="tab-pane fade show p-0">
                            <div class="row g-4">
                                @foreach (var item in ViewBag.LifeStyleNewsimg)
                                {
                                    <div class="col-lg-8">
                                        <div class="position-relative rounded overflow-hidden">
                                            @if (item.image != null)
                                            {
                                                var base64 = Convert.ToBase64String(item.image);
                                                var imgsrc = string.Format("data:image/jpg;base64,{0}", base64);
                                                <img alt="@item.image" src='@imgsrc' class="img-zoomin img-fluid rounded w-100" alt="">
                                            }
                                            
                                            <div class="position-absolute text-white px-4 py-2 bg-primary rounded" style="top: 20px; right: 20px;">
                                                ফ্যাশন
                                            </div>
                                        </div>
                                        <div class="my-3">
                                            @Html.ActionLink((string)@item.title, "Details", "Posts", new { id = @item.id }, new { @class = "h5 mb-0" })
                                        </div>
                                        <p class="mt-4">
                                            @Html.ActionLink((string)@item.slug, "Details", "Posts", new { id = @item.id }, new { @class = "text-dark mb-0 link-hover" })
                                        </p>
                                        <div class="d-flex justify-content-between">
                                            <!--<a href="#" class="text-dark link-hover me-3"><i class="fa fa-clock"></i> 06 minute read</a>
                                            <a href="#"><a href="#" class="text-dark link-hover me-3"><i class="fa fa-eye"></i> 3.5k Views</a>
                                            <a href="#" class="text-dark link-hover me-3"><i class="fa fa-comment-dots"></i> 05 Comment</a>
                                            </a> href="#" class="text-dark link-hover"><i class="fa fa-arrow-up"></i> 1.5k Share</a>-->
                                        </div>
                                        

                                    </div>
                                    }
                                <div class="col-lg-4">
                                    <div class="row g-4">
                                        @foreach (var item in ViewBag.LifeStyleNews)
                                            {
                                                <div class="col-12">
                                                    <div class="row g-4 align-items-center">
                                                        <div class="col-5">
                                                            <div class="overflow-hidden rounded">
                                                                @if (@item.image != null)
                                                                {
                                                                    var base64 = Convert.ToBase64String(@item.image);
                                                                    var imgsrc = string.Format("data:image/jpg;base64,{0}", base64);

                                                                    <img alt="@item.image" src='@imgsrc' class="img-zoomin img-fluid rounded w-100" alt="">
                                                                }
                                                                
                                                            </div>
                                                        </div>
                                                        <div class="col-7">
                                                            <div class="features-content d-flex flex-column">
                                                                <p class="text-uppercase mb-2">@item.Category.name</p>
                                                                @Html.ActionLink((string)@item.title, "Details", "Posts", new { id = @item.id }, new { @class = "h5 mb-0" })
                                                            <small class="text-body d-block"><i class="fas fa-calendar-alt me-1"></i>@item.RecordDate.ToString("dd-MM-yyyy")</small>
                                                            </div>
                                                        </div>
                                                    </div>
                                                </div>
                                            }
                                            

                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                    <div class="border-bottom mb-4">
                        <h2 class="my-4">সর্বাধিক দেখা খবর</h2>
                    </div>
                    <div class="whats-carousel owl-carousel">
                        @foreach (var item in ViewBag.postByViews)
                         {
                            <div class="latest-news-item">
                                <div class="bg-light rounded">
                                    <div class="rounded-top overflow-hidden">
                                        @if (@item.image != null)
                                        {
                                            var base64 = Convert.ToBase64String(@item.image);
                                            var imgsrc = string.Format("data:image/jpg;base64,{0}", base64);

                                            <img alt="@item.image" src='@imgsrc' class="img-zoomin img-fluid rounded-top w-100" alt="">
                                        }
                                        
                                    </div>
                                    <div class="d-flex flex-column p-4">
                                        <a href="#" class="h4">@item.Category.name</a>
                                        <div class="d-flex justify-content-between">
                                            @Html.ActionLink((string)@item.title, "Details", "Posts", new { id = @item.id }, new { @class = "h5 mb-0" })
                           
                                        </div>
                                        <div class="col-8">
                                            <div class="features-content d-flex flex-column">
                                                <small class="text-body d-block" style="font-size: 9pt"><i class="fas fa-calendar-alt me-1"></i>@item.RecordDate.ToString("dd-MM-yyyy")</small>
                                            </div>
                                        </div>
                                    </div>
                                </div>
                            </div>
                         }
                    </div>
                    <div class="mt-5 lifestyle">
                        <div class="border-bottom mb-4">
                            <h1 class="mb-4">লাইফ স্টাইল</h1>
                        </div>
                        <div class="row g-4">
                         @foreach (var item in ViewBag.LifeStyleNews)
                            {
                            <div class="col-lg-6">
                                <div class="lifestyle-item rounded">
                                   
                                    @if (@item.image != null)
                                    {
                                        var base64 = Convert.ToBase64String(@item.image);
                                        var imgsrc = string.Format("data:image/jpg;base64,{0}", base64);

                                        <img alt="@item.image" src='@imgsrc' class="img-fluid w-100 rounded" alt="">
                                    }
                                <div class="d-flex flex-column p-4">
                                    <a href="#" class="h4">@item.Category.name</a>
                                    <div class="d-flex justify-content-between">
                                        @Html.ActionLink((string)@item.title, "Details", "Posts", new { id = @item.id }, new { @class = "h5 mb-0" })

                                    </div>
                                    <div class="col-8">
                                        <div class="features-content d-flex flex-column">
                                            <small class="text-body d-block" style="font-size: 9pt"><i class="fas fa-calendar-alt me-1"></i>@item.RecordDate.ToString("dd-MM-yyyy")</small>
                                        </div>
                                    </div>
                                </div>
                                </div>
                            </div>
                            }
                        </div>
                    </div>
                </div>
                <div class="col-lg-4 col-xl-3">
                    <div class="row g-4">
                        <div class="col-12">
                            <div class="p-3 rounded border">
                                <h4 class="mb-4">Stay Connected</h4>
                                <div class="row g-4">
                                    <div class="col-12">
                                        <a href="#" class="w-100 rounded btn btn-primary d-flex align-items-center p-3 mb-2">
                                            <i class="fab fa-facebook-f btn btn-light btn-square rounded-circle me-3"></i>
                                            <span class="text-white">13,977 Fans</span>
                                        </a>
                                        <a href="#" class="w-100 rounded btn btn-danger d-flex align-items-center p-3 mb-2">
                                            <i class="fab fa-twitter btn btn-light btn-square rounded-circle me-3"></i>
                                            <span class="text-white">21,798 Follower</span>
                                        </a>
                                        <a href="#" class="w-100 rounded btn btn-warning d-flex align-items-center p-3 mb-2">
                                            <i class="fab fa-youtube btn btn-light btn-square rounded-circle me-3"></i>
                                            <span class="text-white">7,999 Subscriber</span>
                                        </a>
                                        <a href="#" class="w-100 rounded btn btn-dark d-flex align-items-center p-3 mb-2">
                                            <i class="fab fa-instagram btn btn-light btn-square rounded-circle me-3"></i>
                                            <span class="text-white">19,764 Follower</span>
                                        </a>
                                        <a href="#" class="w-100 rounded btn btn-secondary d-flex align-items-center p-3 mb-2">
                                            <i class="bi-cloud btn btn-light btn-square rounded-circle me-3"></i>
                                            <span class="text-white">31,999 Subscriber</span>
                                        </a>
                                        <a href="#" class="w-100 rounded btn btn-warning d-flex align-items-center p-3 mb-4">
                                            <i class="fab fa-dribbble btn btn-light btn-square rounded-circle me-3"></i>
                                            <span class="text-white">37,999 Subscriber</span>
                                        </a>
                                    </div>
                                </div>
                                <h4 class="my-4">জনপ্রিয় খবর</h4>
                                <div class="row g-4">
                                    @foreach (var item in ViewBag.rightSideNews)
                                     {
                                        <div class="col-12">
                                            <div class="row g-4 align-items-center features-item">
                                                <div class="col-4">
                                                    <div class="rounded-circle position-relative">
                                                        <div class="overflow-hidden rounded-circle">
                                                            @if (@item.image != null)
                                                            {
                                                                var base64 = Convert.ToBase64String(@item.image);
                                                                var imgsrc = string.Format("data:image/jpg;base64,{0}", base64);

                                                                <img alt="@item.image" src='@imgsrc' class="img-zoomin img-fluid rounded-circle w-100" alt="">
                                                            }
                                                        </div>
                                                        <!--<span class="rounded-circle border border-2 border-white bg-primary btn-sm-square text-white position-absolute" style="top: 10%; right: -10px;">3</span>-->
                                                    </div>
                                                </div>
                                                <div class="col-8">
                                                    <div class="features-content d-flex flex-column">
                                                        <p class="text-uppercase mb-2" style="font-size:15pt">@item.Category.name</p>
                                                        @Html.ActionLink((string)@item.title, "Details", "Posts", new { id = @item.id }, new { @class = "h5 mb-0" })
                                                        <small class="text-body d-block"><i class="fas fa-calendar-alt me-1"></i>@item.RecordDate.ToString("dd-MM-yyyy")</small>
                                                    </div>
                                                </div>
                                            </div>
                                        </div>
                                     }                    
                                    <div class="col-lg-12">
                                        <a href="#" class="link-hover btn border border-primary rounded-pill text-dark w-100 py-3 mb-4">View More</a>
                                    </div>
                                    <div class="col-lg-12">
                                        <div class="border-bottom my-3 pb-3">
                                            <h4 class="mb-0">Trending Tags</h4>
                                        </div>
                                        <ul class="nav nav-pills d-inline-flex text-center mb-4">
                                            <li class="nav-item mb-3">
                                                <a class="d-flex py-2 bg-light rounded-pill me-2" href="#">
                                                    <span class="text-dark link-hover" style="width: 90px;">লাইফ স্টাইল</span>
                                                </a>
                                            </li>
                                            <li class="nav-item mb-3">
                                                <a class="d-flex py-2 bg-light rounded-pill me-2" href="#">
                                                    <span class="text-dark link-hover" style="width: 90px;">খেলাধুলা</span>
                                                </a>
                                            </li>
                                            <li class="nav-item mb-3">
                                                <a class="d-flex py-2 bg-light rounded-pill me-2" href="#">
                                                    <span class="text-dark link-hover" style="width: 90px;">প্রযুক্তি</span>
                                                </a>
                                            </li>
                                            <li class="nav-item mb-3">
                                                <a class="d-flex py-2 bg-light rounded-pill me-2" href="#">
                                                    <span class="text-dark link-hover" style="width: 90px;">ম্যাগাজিন</span>
                                                </a>
                                            </li>
                                            <li class="nav-item mb-3">
                                                <a class="d-flex py-2 bg-light rounded-pill me-2" href="#">
                                                    <span class="text-dark link-hover" style="width: 90px;">খেলা</span>
                                                </a>
                                            </li>
                                            <li class="nav-item mb-3">
                                                <a class="d-flex py-2 bg-light rounded-pill me-2" href="#">
                                                    <span class="text-dark link-hover" style="width: 90px;">সিনেমা</span>
                                                </a>
                                            </li>
                                            <li class="nav-item mb-3">
                                                <a class="d-flex py-2 bg-light rounded-pill me-2" href="#">
                                                    <span class="text-dark link-hover" style="width: 90px;">ভ্রমণ</span>
                                                </a>
                                            </li>
                                            <li class="nav-item mb-3">
                                                <a class="d-flex py-2 bg-light rounded-pill me-2" href="#">
                                                    <span class="text-dark link-hover" style="width: 90px;">বিশ্ব</span>
                                                </a>
                                            </li>
                                        </ul>
                                    </div>
                                    <div class="col-lg-12">
                                        <div class="position-relative banner-2">
                                            <img src="~/assest/img/banner-2.jpg" class="img-fluid w-100 rounded" alt="">
                                            <div class="text-center banner-content-2">
                                                <h6 class="mb-2">The Most Populer</h6>
                                                <p class="text-white mb-2">News & Magazine WP Theme</p>
                                                <a href="#" class="btn btn-primary text-white px-4">Shop Now</a>
                                            </div>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
<!-- Most Populer News End -->
